name: rp2 port

on:
  push:
  pull_request:
    paths:
      - '.github/workflows/*.yml'
      - 'tools/**'
      - 'py/**'
      - 'extmod/**'
      - 'shared/**'
      - 'lib/**'
      - 'drivers/**'
      - 'ports/rp2/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 'micropython repo'  # test build with space in path
    steps:
    - uses: actions/checkout@v7
      with:
        path: 'micropython repo'
    - name: Install packages
      run: tools/ci.sh rp2_setup
    - name: Build
      run: tools/ci.sh rp2_build
    - name: Kész firmware kimentése
      uses: actions/upload-artifact@v4
      with:
        name: pico2w-firmware
        path: 'micropython repo/ports/rp2/build-RPI_PICO2_W/*.uf2'
